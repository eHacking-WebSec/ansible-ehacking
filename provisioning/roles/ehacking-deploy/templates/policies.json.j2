{
  "policies": {
    "Certificates": {
      "ImportEnterpriseRoots": true,
      "Install": [
        {{ query('fileglob', '/usr/local/share/ca-certificates/*.crt') | map('regex_replace', '^', '"') | map('regex_replace', '$', '"') | join(',\n        ') }}
      ]
    }
  }
}
